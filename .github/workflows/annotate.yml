name: Annotate

on:
  workflow_dispatch

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Create Annotations
      run: |
        echo ::error line=2, endLine=1::This will fail
