on:
    push:
        branches:
            -   master
name: Test
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
        - uses: 'actions/setup-python@v2'
          with:
              python-version: 3.8
              
        - run: |
              python -V
