on:
    push:
        branches:
            -   master
name: Test
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
        - run: |
            echo "::set-env name=my_key::my_value"
        - uses: 'actions/hello-world-javascript-action'
          with:
            who-to-greet: 'This should work ${{ my_key }}'

