name: Second workflow
on:
  workflow_run:
    workflows: ["First workflow"]
    branches: [main]
    types: 
      - completed
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "hello"
