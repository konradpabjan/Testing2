on:
  workflow_run:
    workflows: ["python"]
    branches: [main]
    types: 
      - completed
      - requested
name: workflow_run test
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "hello"
